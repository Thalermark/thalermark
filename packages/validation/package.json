{
  "name": "@thalermark/validation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": ["./dist/messages.js", "./src/messages.ts"],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo dist *.tsbuildinfo"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "25.9.1",
    "vitest": "4.1.8"
  }
}
